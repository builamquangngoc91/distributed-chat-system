version: '3.8'
services:
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=socialnetwork
    ports:
      - '5432:5432'
    volumes: 
      - db:/var/lib/postgresql/data
    networks:
      - social_network

  redis:
    image: 'redis:7'
    restart: always
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - '6379:6379'
    volumes:
      - redis:/data
    networks:
      - social_network

  auth:
    container_name: auth_service
    build: ./auth/
    ports:
      - "8080:8080"
    restart: always
    environment:
      SN_DB_PORT: "5432"
      SN_DB_HOST: "db"
      SN_DB_USERNAME: "postgres"
      SN_DB_PASSWORD: "postgres"
      SN_DB_NAME: "socialnetwork"
      SN_RD_HOST: "redis"
      SN_RD_PORT: "6379"
      SN_AUTH_SERVICE_JWT_KEY: RtaW4iLCJJc3N
      SN_AUTH_SERVICE_PORT: "8080"
    depends_on:
      - redis
      - db
    networks:
      - social_network

  chat:
    container_name: chat_service
    build: ./chat/
    ports:
      - "8081:8081"
    restart: always
    environment:
      SN_DB_PORT: "5432"
      SN_DB_HOST: "db"
      SN_DB_USERNAME: "postgres"
      SN_DB_PASSWORD: "postgres"
      SN_DB_NAME: "socialnetwork"
      SN_RD_HOST: "redis"
      SN_RD_PORT: "6379"
      SN_CHAT_SERVICE_PORT: "8081"
      SN_AUTH_SERVICE_BASE_URL: "http://auth:8080"
    depends_on:
      - redis
      - db
    networks:
      - social_network

volumes:
  db:
    driver: local
  redis:
    driver: local

networks:
  social_network:
